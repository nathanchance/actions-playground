name: 5.15 (clang-16)
on:
  push:
  workflow_dispatch: null
jobs:
  test:
    name: "Test"
    runs-on: ubuntu-latest
    steps:
    - name: test
      run: |
        echo "${{ github.workflow }}"
        echo "$GITHUB_WORKFLOW"
